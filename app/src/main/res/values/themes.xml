<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la aplicación. Usamos pantalla completa sin ActionBar propia del sistema. -->
    <style name="Base.Theme.Loginlayout" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/steam_bg_dark</item>
        <item name="android:statusBarColor">@color/steam_bg_dark</item>
    </style>

    <style name="Theme.Loginlayout" parent="Base.Theme.Loginlayout" />

</resources>